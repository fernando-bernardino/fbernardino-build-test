Created by: Fernando Bernardino <tony.bernardino@gmail.com>

Application in Spring Boot with one REST endpoint:

GET /hello/{name}

will return:

{"msg": "Hello 'name'"}, where 'name' is the path variable passed in the URL
